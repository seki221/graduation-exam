<% content_for(:title, t('.title')) %>

<div class="container pt-3">
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @favorite_schedules.present? %>
          <%= render @favorite_schedules %>
          <%= paginate @favorite_schedules, theme: 'bootstrap-5'%>
        <% else %>
          <p><%= t('.no_result') %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>