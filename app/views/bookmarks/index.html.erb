<% content_for(:title, t('.title')) %>
itirann
<div class="container pt-3">
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @bookmark_schedules.present? %>
          <%= render @bookmark_schedules %>
          aa
        <% else %>
          <p><%= t('.no_result') %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>